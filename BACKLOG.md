# BACKLOG — 검토했지만 아직 안 한 것들

> 비판적 검토(2026-06) 때 나온 아이디어 중 **의도적으로 미룬 것**. 시작할 땐 이 파일에서 꺼내 가고, 끝나면 줄을 지운다.

## 1. 에이전트 구성 가이드
- 무엇: 해커톤에서 서브에이전트를 **어떻게 구성**할지(역할 분담 예: 빌더/데이터 검증/리뷰어) — `.claude/agents/` 정의 예시 동봉까지.
- 왜 미룸: 현재 `claude-craft/subagents.md` + `method/README.md` §4로 기본은 충분. 드라이런에서 멀티에이전트가 실제로 필요했던 적이 아직 없음(MAST: 기본은 단일).
- 시작 조건: 드라이런 중 "병렬이 진짜 필요했다"는 순간이 나오면.

## 2. 서비스 유형 확장 (A·B·C·D 너머)
- 후보: **E 모니터링/알림형**(상태 변화 감지 → 통지), **F 비교/선택형**(N개 대안 나란히 → 결정 지원), **G 학습/연습형**(반복 훈련 + 피드백).
- 왜 미룸: 유형은 **드라이런으로 검증된 것만** 추가한다는 원칙(A~D 전부 드라이런 거침). 머리로 만든 유형은 환류 없는 추측.
- 시작 조건: 해당 유형의 실제 문제로 드라이런 1회.

## 3. 기존 교훈의 조건→처방 형식 통일
- 무엇: 초기 환류분(일부 서사 섞임)을 "이런 특징이면 → 이렇게" 형식으로 재정리.
- 왜 미룸: 크게 어긋나지 않아 비용 대비 이득 낮음(그릴 세션 합의).

## 4. design-polish 스킬 도그푸딩
- 무엇: 드라이런 앱 1개에 동봉 스킬을 실제로 돌려 스킬 자체를 검증·개선.

## ~~6. FastHTML 템플릿 승격~~ ✅ 완료 (2026-06-11)
- 검증 완료: ① 재구현 비교(4분57초·환각0·토큰 주입 시 제품감 동급) ② **그린필드 시연 3분55초** — 멀티페이지 위저드·세션 누적·Leaflet 지도 입력·Three.js 3D까지(`playbook-dryrun-fasthtml-showcase`).
- 완료: `stack/templates/fasthtml_app.py` 동봉(위저드+tokens.css+llms-ctx 안내, e2e 검증). **기본값 교체도 완료**(2026-06-11, 사용자 결정) — FastHTML이 기본 스택, Streamlit은 최소 코드 대안.
- 적극 검토 방침(사용자 결정): LLM+제품감이 둘 다 필요한 문제에선 FastHTML을 우선 후보로.

## 5. UX 수입처 확장
- 무엇: 토스 외 — 당근(지역 맥락), 배민(라이팅 톤) 등 한국 서비스의 공개 디자인 자료 선별 수입.
- 규칙 유지: **토스 기준, 충돌 없는 조언만 병합**(`lessons/ux.md` 참조 우선순위).

## 7. 설계 렌즈 후보: '위임/대리'로 마찰 단계 제거
- 무엇(일반 아이디어): 사용자가 직접 하기 어려운/마찰 큰 단계(서류 발급, 인증, 신청 대행)를 **본인이 하게 두지 말고** — (a) **권한 위임**으로 제3자(담당자·대리인·시스템)가 대신 처리하거나, (b) 애초에 **안 해도 되게**(기관 간 자동연동·정보 공동이용 = 제출 면제) 설계하면 문제 자체가 사라진다. Rule⑤(정보를 만들지 말고 행동화)·데이터 원칙(판정은 코드/기관)과 결을 같이함. 파이프라인의 Action Planning/Artifact를 "할 일 목록"에서 "위임장 초안 + 면제 경로 안내"로 한 단계 끌어올리는 발상.
- 어디서 나옴: welfare-finder 도그푸딩 — "필요 서류를 담당자가 대신 떼주면 사실상 많은 문제가 해결". 복지뿐 아니라 **어떤 행정/신청 문제에도 적용** 가능한 범용 렌즈.
- 왜 미룸: 머리로 만든 패턴은 추측(§2 원칙). **드라이런으로 검증 전**. 법적 효력·실제 대리발급/공동이용 가능 서류의 출처 확인도 필요.
- 시작 조건: 위임/대리가 핵심인 실제 문제로 드라이런 1회 → 검증되면 `patterns/`(새 모듈) 또는 `lessons/`로 승격.

## 8. 설계 렌즈 후보: '현황 지갑'으로 라이프사이클 닫기
- 무엇(일반 아이디어): 발견·추천·실행으로 **끝내지 말고**, 실행 이후의 상태(신청함/심사중/수급중, 잔여·한도, 갱신·재확인 기한)를 담는 **사후 관리 레이어**를 고려한다. 일회성 도구를 **재방문 가치 있는 것**으로 바꾼다. `lessons/dashboard.md`(대시보드 교훈)·Rule④(결과물 생성)의 연장선. 신뢰 원칙 유지 — 우리가 금액·자격을 *판정/계산*하지 않고 사용자가 넣거나 공식 출처에서 확인한 값을 **담아 보여줄 뿐**, 저장은 로컬 우선(개인정보 회피).
- 어디서 나옴: welfare-finder 도그푸딩 — "바우처 지갑처럼 얼마 신청했고 얼마 남았는지 보이면 관리가 쉬워진다". 복지 외 — 신청·구독·한도가 있는 어떤 도메인에도 적용.
- 왜 미룸: 검증 전 아이디어. 사후 관리는 **재방문/지속 사용**을 전제하는데, 해커톤·드라이런의 '한 시나리오 완주' 범위를 넘음(범위 비용 큼).
- 시작 조건: 라이프사이클이 핵심인 문제로 드라이런하거나, 기존 드라이런에 "끝맺음 이후가 진짜 가치"라는 신호가 나오면.
